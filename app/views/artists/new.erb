<h1>
    <%= @s == 'new' ? "Create Artist!" : "Edit Artist!" %>
</h1>

<%= form_for @artist do |f| %>
    <%= f.text_field :name %>
    <%= f.text_field :bio %>
    <%= f.submit (@s == 'new' ? 'Create Artist' : 'Update Artist') %>
<% end%>